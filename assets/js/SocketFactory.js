// Generated by CoffeeScript 1.7.1
(function() {
  Application.core.factory('socket', function($rootScope) {
    var del, emit, get, onn, post, put, socket;
    socket = io.connect();
    onn = function(eventName, callback) {
      return socket.on(eventName, function() {
        var args;
        args = arguments;
        return $rootScope.$apply(function() {
          return callback.apply(socket, args);
        });
      });
    };
    emit = function(eventName, data, callback) {
      return socket.emit(eventName, data, function() {
        var args;
        args = arguments;
        return $rootScope.$apply(function() {
          if (callback) {
            return callback.apply(socket, args);
          }
        });
      });
    };
    get = function(path, param, cb) {
      return socket.get(path, param, cb);
    };
    post = function(path, param, cb) {
      return socket.post(path, param, cb);
    };
    put = function(path, param, cb) {
      return socket.put(path, param, cb);
    };
    del = function(path, param, cb) {
      return socket["delete"](path, param, cb);
    };
    return {
      'on': onn,
      'emit': emit,
      'get': get,
      'post': post,
      'put': put,
      'delete': del
    };
  });

}).call(this);

//# sourceMappingURL=SocketFactory.map
